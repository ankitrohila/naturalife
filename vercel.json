{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && DATABASE_URL=$DATABASE_URL_UNPOOLED npx prisma migrate deploy && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm ci",
  "regions": ["bom1"]
}
