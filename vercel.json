{
  "framework": "nextjs",
  "buildCommand": "npx prisma db push --skip-generate && npx prisma generate && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm ci",
  "regions": ["bom1"]
}
